#include <iostream> // include the input-output stream library

int main() { //always int main coz program needs to return an integer to let know the program result ended 
    std::cout << "Hello, C++17 World!" << std::endl;
    return 0; // error code for OS to indicate that program ended successfully
}